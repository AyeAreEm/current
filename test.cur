extern malloc :: fn(size: usize) *void;
extern free :: fn(mem: *void) void;
extern puts :: fn(s: cstring) i32;

main :: fn() void {
    mem := malloc(1);
    defer free(mem);

    puts(c"freeing memory");
}
