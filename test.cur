extern puts :: fn(s: cstring) i32;
extern malloc :: fn(size: usize) *void;
extern free :: fn(ptr: *void) void;

main :: fn() void {
    puts(c"hello world");

    num := cast(*i32)malloc(sizeof(i32));
    defer free(cast(*void)num);
    num.& = 10;
}
